import {View, Image} from 'react-native';
import {  } from 'react-native';

const SavedImages = (images: any) => {
  return (
    {/* <View>
      {
        images.map((image: string) => {
          return <Image source={{uri: image}} resizeMode='contain' className='!h-24 !w-24' />
        }) 
      }
    </View> */}
  );
}

export default SavedImages;